// Espera a que el DOM esté completamente cargado
/*document.addEventListener("DOMContentLoaded", function () {
    // Obtén todas las cajas de series
    const serieBoxes = document.querySelectorAll(".serie-box");

    // Itera sobre cada caja de serie
    serieBoxes.forEach(function (box) {
        // Añade un evento de mouseover a cada caja
        box.addEventListener("mouseover", function () {
            // Muestra la superposición al pasar el ratón
            const overlay = this.querySelector(".overlay");
            overlay.style.opacity = "1";
        });

        // Añade un evento de mouseout a cada caja
        box.addEventListener("mouseout", function () {
            // Oculta la superposición al salir del ratón
            const overlay = this.querySelector(".overlay");
            overlay.style.opacity = "0";
        });
    });
});*/